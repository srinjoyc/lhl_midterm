<%
    @defender = User.find(@encounter.defender_id)
%>

<h1>Challenge accepted.</h1>

  <div class="col-md-4 panel">

    <div class="panel-heading">
      <h2>Defender:</h2>
      <h3><%= @defender.username %></h3>
    </div>

    <div class="well">
      <ul>
        <li>Area: <%= @encounter.location %></li>
        <li>Provided Address: [Address]

              <%= #This will at some point contain
                  # @defender.address, when that's a
                  # value that we ask for during signup
              %>
        </li>
      </ul>
    </div>
  </div>

  <div class="col-md-4 panel">
    <div class="panel-heading">
      <h2>Attacker:</h2>
    <h3>
    <% if @encounter.attacker_id %>
      <% @attacker = User.find(@encounter.attacker_id) %>
      Cato: <%= @attacker.username %>
    <% else %>
      Cato Unassigned
    <% end %>
    </h3>
  </div>
    <div class="well">
      <ul>
        <li>Default data</li>
        <li>It would be really great</li>
        <li>  to fill this with some stuff</li>
        <li>    just like whenever we get around to it.</li>
      </ul>
    </div>
  </div>

<% if @current_user %>

<% end %>
<a href="/">Go back to home page</a>
